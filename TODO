Общие:
+ После отключения клиента все опубликованные им топики удаляются?
  Надо руками удалять.

☐ CreateDevice при создании уже существующего устройства сносит все его подтемы или нет?
  Не ясно. Вроде должно, но надо проверить.

☐ Что будет, если закрыть mqtt клиент, а в соседнем потоке фигачить SetRawValue?
  ХЗ

+ Бывают ли отрицательные значения в in_voltageX_raw
  ХЗ. Добавил чтение в int32_t

☐ Клиент mqtt сам делает реконнект к брокеру? Если в этот момент мы пытаемся писать в топик, будет исключение или что?

+ RemoveDevice удаляет все подтопики?
  Да

+ Убрать .vscode из репы

По либе:
☐ Зачем в TLocalDeviceArgs unique_ptr на string? Почему не просто string?
☐ Зачем в MakeThread std::function? 
☐ нет <functional> в utils.h
☐ при неудачном подключении к mqtt вываливается
  terminate called without an active exception
  qemu: uncaught target signal 6 (Aborted) - core dumped
  Aborted (core dumped)
  По хорошему надо корректно закрытие обрабатывать, но это где-то в кишках либы.

☐ WBMQTT::SignalHandling::Stop();
  WBMQTT::SignalHandling::Wait();
  Зависон. Надо, чтобы Wait проверял, что SignalHandling не работает.
  
+ Что делать с невалидным конфигом? Падать и писать в лог? Использовать какой-то конфиг по умолчанию? 
  Что делать, если один из конфигов в списке не валидный? Пропускать и работать без него или падать и писать в лог?
  Что делать, если один из параметров не валидный? Устанавливать ближайшее валидное значение или падать?
  Падать и писать в лог при любой ошибке в конфигах.

+ MatchIIO надо или всегда iio:device0?
  Надо ли оставлять вызов of_find_iio_dev_links, который делает в настройках поля "match_iio": "../../../devices/soc0/soc/2100000.aips-bus/2198000.adc/iio:device0" ?
  Может быть ещё что-то отличное от /bus/iio/devices/iio:device0?
  Надо

- Надо на не нахождение канала в iio писать error в mqtt
- Надо обрабатывать ошибки чтения из файла устройства и пытаться продолжать читать, но выставлять ошибку в mqtt
- На превышение напруги надо ставить ошибку в mqtt
+ Проверить не затирается ли wb-homa-adc.conf при установке пакета.
  Не затирается.

- Что означает настройка max_voltage? В коде она ограничивает значение после умнодения на scale и до max_voltage?
